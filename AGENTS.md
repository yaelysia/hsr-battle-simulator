# AGENTS.md - HSR 工作区入口

每次都用简体中文回复。

本项目构建可审计的《崩坏：星穹铁道》战斗模拟器。目标是基于固定数据来源完成战斗配置、状态转移、快照、结算、replay 和来源审计，最终供外部推演器查询与操作。

## 主线与事实来源

当前主线：

```text
hsr_v075_baseline_clean/hsr/simulator_v8_clean_core/
```

唯一规则来源链：

```text
turnbasedgamedata-main -> compiler/lowering -> Canonical IR -> Combat Core
```

旧 v7 和 `model_pack_v3_0` 仅可作对照，不是兼容目标，也不能成为 v8 runtime 依赖。优先级固定为：

```text
最终成品完整性 > 内核干净程度 > 来源可追查 > 验证可重复 > 旧版兼容
```

## 按需读取

禁止新线程默认通读全部阶段计划和历史报告。只读当前任务需要的最小集合：

- 新线程定向：`hsr_v075_baseline_clean/hsr/CODEX_HANDOFF.md`
- 架构或共享内核修改：`simulator_v8_clean_core/ARCHITECTURE_BOUNDARY_CONTRACT.md`、`FORBIDDEN.md`
- 阶段实施：当前阶段执行卡及其直接依赖，不读相邻阶段卡
- 规划、验收或验证改造：`simulator_v8_clean_core/docs/AGENT_WORKFLOW_AND_VALIDATION.md`
- 查文档归属：`simulator_v8_clean_core/DOCUMENTATION_INDEX.md`
- 查历史决策：先在计划、报告和 `docs/archive/` 中按关键词搜索，不全量读取

当前状态只在 `CODEX_HANDOFF.md` 维护，本文件不再复制 P1-P8 历史。

## 架构红线

- runtime 只能读取 Canonical IR、数据卡 IR 和已装配的正式输入。
- raw TBGD、TextMap 和机制文本只能进入 compiler/lowering/discovery/审计或 UI 展示层。
- 禁止使用旧 v7、旧 model pack、观测伤害和手工答案补规则。
- 禁止为角色、怪物、光锥、遗器、关卡或路线在核心系统写专属特判。
- 禁止复活 `BattleSimulator`、`SimulatorRuntimeAdapter`、`_legacy_effects`、`action_ctx`。
- 未知、缺来源、缺条件、缺目标、缺公式或缺事件 payload 必须 blocked，并保持 state unchanged。
- `audit_only`、`discovered_only`、`blocked` 和 placeholder 不得产生 mutation。
- `source_trace` 只用于审计，不能作为 runtime 行为输入。
- 每个 mutation 必须能追溯到真实 Canonical IR 节点和 TBGD 来源。
- settlement 必须由执行路径原生生成，不能从日志事后反推。
- UI 只能展示内核事实并提交选择，不能计算技能可用性、目标、资源、伤害或状态规则。
- 外部推演器像玩家一样查询、选择和读取结果，不负责敌方 AI 或第二套战斗逻辑。
- 角色卡、怪物卡、光锥卡、遗器卡是内容来源；构筑装配器负责组合，runtime 不理解 raw 构筑规则。
- 光锥和遗器输入表示已经完成的战斗构筑；core 不模拟掉落、背包、养成或强化历史，也不以初始词条数、强化节点或整件总 roll 预算拒绝自定义成品。只校验战斗所需的定义引用、槽位与属性冲突，以及每项输入自身的来源和数值范围。
- 动作归属可行动单位数据卡，目标规则归属 action definition。
- 角色召唤物归属角色卡；怪物召唤物优先复用怪物卡，并额外记录 owner/summoner 和生命周期。
- 不按角色名、怪物名、技能名、固定 ID、固定文件名、固定 hash 或观测答案驱动正式逻辑或主验证选样。

详细禁令以 `FORBIDDEN.md` 为准；发生冲突时选择更严格、来源更真实的解释。

## 战斗范围裁决

本项目模拟战斗，不复刻完整游戏产品。raw 数据存在某种记录，不等于该记录必须进入
正式实现。是否纳入范围取决于它是否改变：

- 合法战斗配置和进入战斗时的初始状态。
- 战斗中的可选动作、目标、时机或资源。
- 状态转移、数值结算、胜负结果、settlement 或 replay。

获取、掉落、定向生成、合成、背包、商店、养成流程、展示和服务端管理等非战斗过程
默认不实现；若它们的最终产物会进入战斗，只接收并校验战斗所需的规范化结果，不复刻
产物生成过程。

角色来源闭包必须同时覆盖角色主能力、角色拥有的派生战斗单位、角色拥有的战斗事件和
共享角色运行时定义。共享规则只 lower 一次，不得复制到每张角色卡。角色最终等级、
晋阶、行迹、星魂和技能等级属于构筑输入；经验、材料、解锁路线和升级历史不属于战斗模拟。

角色来源按完整分支裁决，不能只看节点或事件名称。表现分支中的通用战斗指令仍归表现；
名称像演出的回调若实际产生伤害、状态或行动变化，仍归战斗。UI 和输入控制节点只有在
等价的动作、目标、资源或单位拓扑契约已经投影后才能排除，不能直接静默删除。

全角色机制工程必须先按共享根因归并，再选择代表纵切。旧 blocked 数量、角色复杂度分组
和玩法标签不能直接生成执行顺序：同一状态指令因事件、条件或来源准入重复阻断时，先修
共享事件/表达式/接线边界，不得按角色或报错字符串复制 handler。代表角色数量不得预设，
只能由“未闭合机制包与真实来源形状”的实时覆盖矩阵决定；代表纵切不能替代全目录审计。

执行中遇到范围不明的真实内容时：

- 不得因来源存在就自行扩展兼容，也不得静默丢弃或伪装成普通记录。
- 先保留来源、明确分类并 fail-closed；互不依赖的工作可以继续。
- 记录一条待裁决项，说明它在游戏中是什么、当前为何被阻断、是否影响战斗、实现成本
  和架构影响，并给出纳入、延期、仅目录保留或排除的建议。
- 若继续实施会迫使架构选择、制造错误规则或污染已验收契约，立即暂停；否则最迟在阶段
  验收前提交用户裁决，未裁决不得宣称相关范围完成。

## 工程方式

- 从第一性原理判断问题，不因用户或旧文档已有结论而跳过代码事实。
- 非必要不扫描整个项目。结构问题先用 CodeGraph；字面文本、路径和日志先用 `rg`。
- 修改前检查调用链和影响范围；优先根因修复，不为旧接口污染新内核。
- 信息无法从工作区发现且会改变关键规则时，暂停并询问。
- 新依赖、下载外部代码或创建新 uv 环境必须先取得用户同意。
- 优先复用现有 uv 环境；禁止未经允许覆盖或新建环境。
- 不回滚用户修改，不提交无关文件，不使用破坏性 Git 命令。
- 手工编辑使用 `apply_patch`。
- 运行产物写入 `/tmp`，不提交缓存、`.pyc`、`__pycache__` 或大体积临时输出。

## 阶段协作

- 大型目标拆成严格顺序阶段，一个线程一次只实施一张已确认执行卡。
- 选择较低成本模型执行前，规划线程必须先消除架构裁决。若一张卡同时要求新建数据模型、
  确立生产权威、迁移共享消费者并改变 RNG/event/replay 等高风险语义，不能靠增加说明强行
  下发，应按可独立验收的架构边界拆卡；最后的共享语义收口仍使用足够强的模型。
- 面向 Terra 等执行模型的卡必须给出权威范围选择规则、精确起始符号、已经确定的语义、
  不得自行决定的事项、停止条件和验证预算。不得让执行线程通过全项目扫描重新发现阶段范围，
  也不得用固定数量、ID 或历史验证器替代生产范围契约。
- Codex 内部实施优先由规划/验收线程派生干净上下文的执行代理；模型由修改复杂度、
  共享边界风险和业务重要性决定。规划/验收线程负责派发、复核、修复回传和最终提交，
  用户只处理产品范围或机制语义裁决。
- 派生执行代理前，父线程必须先用 CodeGraph 建立一页以内的临时导航摘要：精确起始
  符号、现有调用链、已确认事实、真正待查问题和禁止作为模板的历史路径。子代理默认
  使用干净上下文，最多先做两轮结构定位；只有出现具体未解析依赖时才能扩大阅读，
  否则返回 `context_gap` 由父线程补充，不得习惯性扫描项目或通读历史验证器。
- 执行卡详细目标、验收、红线和验证只保存在文件中；对话不复述整张卡。
- 执行线程只能提交 `ready_for_review`，不能自称完成、勾 checklist、提交 Git或自动进入下一阶段。
- `ready_for_review` 不是“主验证为绿”的别名。提交前必须逐项闭合当前卡目标：非法或矛盾
  状态已由生产构造/提交边界拒绝，受影响的正式调用者已通过 CodeGraph 完成迁移自审，所有
  已知 gap 已按原因和后续归属列账并保留可反查的代表来源；任一项不能证明时只能提交
  `blocked`、`plan_mismatch` 或 `context_gap`。
- 每条新生产不变量只保留一个最小反例证明，目录完整性只保留一个聚合账本。不得用扩大
  matrix、增加并列 CLI 或复制 runtime 逻辑来弥补生产边界缺失。
- 验收线程必须检查生产代码、调用链、负例和真实 evidence，不能只看 `ok=true`。
- 验收一次性汇总当前可发现的阻断；修复线程只读差量问题，不重新展开全部背景。
- 验收通过后由验收线程勾选对应项并提交检查点；纯规划和说明文档默认不单独提交。
- 并行只允许依赖明确、写集合互斥且从同一检查点创建独立 worktree 的阶段。

完整阶段协议见 `docs/AGENT_WORKFLOW_AND_VALIDATION.md`。

## 验证成本

验证分四层：

1. `fast`：`compileall`、本阶段聚焦验证、`git diff --check`。
2. `direct`：只跑本次调用链直接触达的现行契约回归。
3. `catalog`：全量来源或目录覆盖；仅在来源、lowering、目录准入变化时运行。
4. `full`：里程碑聚合；只在重大架构修改、阶段聚合或用户明确要求时运行。

默认只跑 `fast + 必要 direct`。历史阶段验证是当时的 evidence，不自动属于活跃回归。旧脚本若携带过时假设，应迁移仍有效的谓词或退役，而不是让新代码兼容旧验收。

重验证必须串行、低优先级、限峰值并输出到 `/tmp`。默认禁止写完整 Canonical IR、RuleBook、coverage、fidelity 或 transition dump；确需大产物时使用显式开关。

- 验证治理改动必须在同一阶段替换并删除真实重复路径，同时量化构建次数、耗时和峰值内存；只新增注册表、调度器、缓存层或“验证验证器”不算治理成果。
- 第一次复用应局限在一个已确认的重调用链内。至少两个独立真实消费者证明边界相同后，才允许提取跨阶段通用框架。
- 共享重构建前必须先证明单次构建能在预算内完成且没有加载无关领域；只消费少量目录集合时，应在 compiler/lowering 层建立来源真实的窄投影，不能让聚焦验证先构建完整 Canonical IR 再过滤。
- 聚焦验证直接证明生产契约或真实目录结果，不再为大型验证工具另写同等规模的元验证。验证代码总量默认不得因纯治理而净增长。
- 同一业务不变量只能有一条权威验收路径；不得用多个 CLI mode、direct probe 或重复
  matrix 反复证明同一件事。诊断入口不能升级为并列完成门。
- 未触达的 codec、不可变性、fingerprint、scenario admission 等既有契约默认继承；
  只有生产调用链实际变化时才运行对应最小 direct，不得在每个阶段重新复制验证。
- 阶段验证不得为了证明局部功能手工搭建与本阶段无关的完整角色、敌人、动作或
  Canonical IR 世界。缺少可复用生产入口时先收紧阶段边界，不能让验证器承担第二套装配逻辑。
- 阶段验收后应立即明确脚本生命周期；默认作为 `historical_evidence` 冻结，后续阶段
  不继续扩写。确需成为 `active_contract` 时只保留稳定、轻量、单职责的契约切片。
- 执行卡必须为验证入口数、来源扫描次数、墙钟、峰值内存、产物和验证代码规模设预算；
  超预算时暂停并重新划分证据，不得靠增加子命令、拆文件或重复 fixture 绕过。
- family、目录或来源过滤必须在来源投影、IR/RuleBook 构建、场景执行和 evidence 保留之前生效；先构建完整对象再过滤输出不算聚焦验证。
- 业务聚焦验证不得默认重复执行反向顺序、失败隔离、缓存一致性等元探针；这些只允许在对应治理边界首次建立时单独证明，不能附着在每次功能回归中。
- 事件型 evidence 必须保留事件生产前状态、生产 mutation、候选状态和派发结果的同一链路；禁止把一个状态产生的裸 `GameEvent` 拿到无关状态中复用并称为端到端正例。
- 验证器不得手工重组动作计划、伤害、召唤或提交顺序来替代正式生产入口。为验证新增的 core API 必须有非 `tools/` 生产调用者；只有验证器调用的入口不能作为正式 gameplay 闭合证据。
- 当前阶段没有真实生产者、但依赖未来角色卡、怪物卡或关卡内容的机制，应标为外部内容端到端依赖并保留未证明状态；不得用合成 gameplay producer 冒充 `executed`，也不得误报为内核实现失败。
- 业务验证 CLI 的退出码只服从业务 `ok`；治理指标、允许缺口或历史 evidence 不能把业务失败改写为进程成功。
- 临时已知缺口只能作为当前失败集合的上限，必须允许缺口减少至零；未知失败仍须阻断，不能要求旧失败永久存在。

## 提交

- 阶段验收成功后提交一个范围干净的检查点。
- 验收未通过或仍有未获用户接受的语义阻断时不提交完成检查点。
- 工作区存在无关修改时忽略并保留，不混入当前提交。
- 文档、报告和 checklist 的状态必须诚实区分 `executable`、各类 gap、`deferred` 与 `not_proven`。

瘦身前的完整入口和历史经验保存在：

```text
hsr_v075_baseline_clean/hsr/simulator_v8_clean_core/docs/archive/AGENTS_PRE_SLIM_2026-07-24.md
```

只在追溯历史决策时按关键词读取，不能重新作为默认上下文。
