# 验证治理试点归档

本目录保存早期验证治理与状态完整性试点。试点确认了不可变 committed state、生产边界拒绝非法状态
和聚焦 evidence 的价值，也暴露了 registry、meta-validator 与重聚合会扩大成本的问题。

当前工作流已经统一到：

```text
docs/AGENT_WORKFLOW_AND_VALIDATION.md
```

旧计划和执行卡不能恢复为默认验证套餐，也不能要求后续阶段重跑其中的历史聚合。
